<%= form_for ([@gossip, @comment]), :class => 'form-group' do |f| %>

  <b><%= f.label :comment, 'Post a comment'  %></b>
  <%= f.text_area :comment, class:'form-control'  %>
<br/>
  <%= f.submit "Add a comment", class:'btn btn-primary' %>

<%end%>